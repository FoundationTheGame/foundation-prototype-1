using UnityEngine;
using System.Collections;

public class CameraZoomController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}

    // Update is called once per frame
    void Update()
    {
        //float xAxisValue = Input.GetAxis("Horizontal");
        /*float zAxisValue = Input.GetAxis("Zoom");
        if (Camera.current != null)
        {
            
            Camera.current.transform.Translate(new Vector3(0.0f, 0.0f, zAxisValue*5));
            transform.parent.position = new Vector3(Mathf.Clamp(transform.parent.position.x, 210, 820), Mathf.Clamp(transform.parent.position.z, 100, 191), Mathf.Clamp(transform.parent.position.z, 170, 600));
        }
        */
        
    }
}
